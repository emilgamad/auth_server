from django.apps import AppConfig


class AuthStartConfig(AppConfig):
    """
    Django AppConfig for the auth_start app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'auth_start'
